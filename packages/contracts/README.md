# Shared Contracts

Platform-neutral models for exercises, translations, workout plans, sessions, and progress. The web and Flutter clients should consume the same API contract and should not duplicate catalog normalization rules.
